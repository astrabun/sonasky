/** AT Proto collection NSIDs used by this app. */

/** Marker record: always written on a successful submission, used for dedup. */
export const NSID_SUBMISSION = "app.sonasky.form.submission";

/** Default collection for a full response record (`atproto-record` destination). */
export const NSID_RESPONSE_DEFAULT = "app.sonasky.form.response";
